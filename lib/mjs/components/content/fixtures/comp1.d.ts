declare namespace _default {
    namespace conditional {
        const eq: string;
        const when: null;
        const show: string;
    }
    const tags: never[];
    const type: string;
    const html: string;
    const input: boolean;
    const key: string;
}
export default _default;
