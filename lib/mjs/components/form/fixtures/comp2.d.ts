declare namespace _default {
    const key: string;
    const input: boolean;
    const tableView: boolean;
    const src: string;
    const type: string;
    const tags: never[];
    namespace conditional {
        const show: string;
        const when: null;
        const eq: string;
    }
}
export default _default;
