declare namespace _default {
    const label: string;
    const mask: boolean;
    const spellcheck: boolean;
    const tableView: boolean;
    const modalEdit: boolean;
    const multiple: boolean;
    const currency: string;
    const inputFormat: string;
    const truncateMultipleSpaces: boolean;
    const key: string;
    const type: string;
    const input: boolean;
    const delimiter: boolean;
    const defaultValue: null[];
}
export default _default;
