declare namespace _default {
    const label: string;
    const fancyButton: boolean;
    const promptOnDeselect: boolean;
    const tableView: boolean;
    const modalEdit: boolean;
    namespace validate {
        const required: boolean;
    }
    const key: string;
    const type: string;
    const input: boolean;
    const defaultValue: boolean;
}
export default _default;
